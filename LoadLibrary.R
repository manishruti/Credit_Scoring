load.library <- function()
{
      library(lattice)
      library(ggplot2)
      library(caret)
      library(gplots)
      library(ROCR)
      library(dplyr)
  
      library(rpart)
      library("grid")
      library("MASS")
      library("neuralnet")
      library(kernlab)
      library(randomForest)
      library(class)
      library(sperrorest)
}
